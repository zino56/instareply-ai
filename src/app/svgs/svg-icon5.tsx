/** Check icon (feature lists). */
export default function Icon5({ cid }: { cid: string }) {
  return (
    <svg data-cid={cid} className="w-auto h-4 block shrink-0 max-w-full overflow-hidden" data-component="icon" aria-hidden="true" fill="none" height="16" viewBox="0 0 24 24" width="16">
      <path d="M4 12.5l5 5L20 6.5" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  );
}
